<div class='container my-5'>
  <h3 class='title text-center'>My Courses</h3>
  <table class='table'>
    <thead>
      <tr>
        <th scope='col'>#</th>
        <th scope='col'>Course Name</th>
        <th scope='col'>Level</th>
        <th scope='col'>Created At</th>
        <th scope='col' style='width: 18%;'>Actions</th>
      </tr>
    </thead>
    <tbody>
      {{#each courses}}
        <tr class='align-middle'>
          <th scope='row'>{{sum @index 1}}</th>
          <td>{{this.name}}</td>
          <td>{{this.level}}</td>
          <td>{{this.createdAt}}</td>
          <td class='d-flex justify-content-between'>
            <a href='/courses/{{this.slug}}' class='btn btn-primary btn-sm'>View</a>
            <a href='/courses/edit/{{this._id}}' class='btn btn-secondary btn-sm'>Edit</a>
            <button type='button' class='btn btn-danger btn-sm' onclick="deleteCourse('{{this._id}}')">Delete</button>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <!-- <script>
    function deleteCourse(courseId) {
    if(confirm('Are you sure you want to delete this course?')) {
        fetch('/courses/' + courseId, {
        method: 'DELETE',
        })
        .then(response => response.json())
        .then(data => {
        if(data.success) {
            window.location.reload();
        } else {
            alert('Error deleting course');
        }
        })
        .catch(error => {
        console.error('Error:', error);
        });
    }
    }
    </script> -->
</div>